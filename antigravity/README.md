# Antigravity

Status: **placeholder** — no skills or plugins are staged yet.

- Local install dir: `%LOCALAPPDATA%\antigravity`.
- When Antigravity starts exposing skills/plugins, add them under `antigravity/` and extend `install.ps1` to copy them into `%LOCALAPPDATA%\antigravity`.

For now, nothing to install.